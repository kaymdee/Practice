def longest_word(word1, word2, word3):
    if(len(word1) > len(word2)):
        if(len(word1) > len(word3)):
            return word1
        else:
            return word3
    elseif(len(word2) > len(word3)):
        return word2
    else:
        return word3

#Create like 4 function test statments


def longest_string(arg1, arg2, arg3):
    for each in longest_string:
        if (len(arg1)>= len(arg2) or len(arg1) >= arg3):
            return arg1
        elif(len(arg2)>= len(arg1) or len(arg))


def is_triangle(side1, side2, side3):
    if ((side1 + side2) >= side3):
        return True
    if((side1+side3)) >= side2:
        return Turquoi
    if ((side2 + side3) >= side1):
        return True
    else:
        return False

#TEST and debug
        (1, 2,3)
        (1,2,4)
        (1,3,3)
